import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  minMaxValue: {
    width: 50,
    height: 20,
    backgroundColor: '#e1e4e9',
    borderRadius: 4,
    alignItems: 'center',
    justifyContent: 'center',
  },
  minMaxValueTxt: {color: '#999', fontSize: 12},
});
